# Handoff — Calderyn Shopify App Store submission

**Date:** 2026-06-08 · **Branch:** `main` · **Repo:** `github.com/keyuchen1735-boop/calderyn-shopify-app`
**From:** Eric (this session) · **To:** engineer taking over the App Store submission

---

## TL;DR

The **hard blocker is cleared.** The app no longer lives on the rejected `*.vercel.app` URL — it's on the custom domain **`https://app.calderyncompany.com`**, fully wired (DNS + TLS + OAuth verified), and a new Shopify app version (**`calderynextension-8`**) has been released to users with the updated App URL + redirect URLs.

What's left is (a) one **security must-do**, (b) **committing one config file**, and (c) **finishing + submitting the App Store listing** in the Shopify Partner Dashboard. Items (a)–(c) need the access listed below.

---

## 🔴 Do this FIRST — leaked secret

`.env.example` (a tracked, placeholder-only file) currently contains a **real, live Anthropic API key** in the working tree (key starts `sk-ant-api03-uJf7...`). It is **not committed** — but it's one `git add .` from being pushed.

1. **Rotate/revoke that key** in the Anthropic console — treat it as compromised.
2. Revert `.env.example` so the value is an empty placeholder: `ANTHROPIC_API_KEY=` (and restore `CLAUDE_MODEL` to the intended value; it was changed to `claude-sonnet-4-6`).
3. Real secrets belong in `.env.local` / Vercel env only — never `.env.example` or source (repo rule).

---

## ✅ What's already done this session (verified, not asserted)

| Step | State | Evidence |
|------|-------|----------|
| Custom domain attached | `app.calderyncompany.com` → Vercel project `shopify-app` | `vercel domains inspect` |
| DNS record | `app A 76.76.21.21` added in **Squarespace** DNS (registrar-managed; NOT GCP) | resolves on public resolvers |
| TLS cert | Valid Let's Encrypt cert, HTTPS 200 | `CN=app.calderyncompany.com`, notAfter 2026-09-06 |
| Vercel prod env | `SHOPIFY_APP_URL` set to `https://app.calderyncompany.com` (was empty `""`) | `vercel env ls production` |
| Prod redeploy | New prod deploy serving the domain | `vercel redeploy` → aliased |
| Shopify config | `application_url` + all 3 `redirect_urls` → new domain | `shopify.app.calderynextension.toml` |
| **Release** | **`calderynextension-8` released to users** | `shopify app deploy` success |
| OAuth smoke test | `/auth/login?shop=…` → 302 to `admin.shopify.com/.../oauth/install` w/ correct `client_id` | curl |

**DNS gotcha for future you:** the domain reads as `ns-cloud-*.googledomains.com` (looks like GCP Cloud DNS) but is actually **legacy Google Domains → Squarespace registrar DNS**. It is NOT a GCP Cloud DNS zone in any project — don't waste time in `gcloud`. Edit records in the **Squarespace** domain DNS panel.

---

## 🟡 Uncommitted change to land

`shopify.app.calderynextension.toml` is what Shopify deployed, but it is **not committed** — a clean checkout still has the old `*.vercel.app` URLs and a future `shopify app deploy` from a fresh clone would revert the App URL.

- Commit **only** that file (e.g. `routes/config: point app at app.calderyncompany.com`). Suggest a dedicated branch off `main` since it's infra config, not feature work.
- **Do NOT** commit `.env.example` (leaked key — see above) or `.vercel/remix-build-result.json` (machine-local build artifact regenerated by the deploy).

---

## 📋 Remaining work to finish the App Store (webstore) application

These are completed in the **Shopify Partner Dashboard** for app `CalderynExtension`, not in the repo. Verify current completeness in the dashboard — I couldn't see listing-field state from the CLI:

1. **App Store listing content** — name, tagline, description, app icon, feature media/screenshots, demo video, categories, languages.
2. **Pricing** — plans / pricing details.
3. **Required URLs** — privacy policy URL, support/contact email, FAQ. These must point at live, reachable pages (now possible on `app.calderyncompany.com` or the marketing site `calderyncompany.com`).
4. **App setup / install flow** — confirm the test install on `calderyn-shop-tester.myshopify.com` works through the new domain end-to-end (OAuth verified; do a full install + embedded load).
5. **Submit for review** — submit the listing for Shopify App Review; respond to reviewer feedback.

> Production-readiness blockers (separate from listing submission, but gate real client data) are tracked in **`DEPLOYMENT_READINESS.md`** — notably prod↔repo migration drift, the inert action-retry executor registry, and the `ad_spend_fact` conflict key. Review before opening to real merchants.

---

## 🔑 Access the coworker needs to finish

| # | System | What to grant | Why | How |
|---|--------|---------------|-----|-----|
| 1 | **Shopify Partner org "Calderyn"** | Add as org member with app management / distribution access to app `CalderynExtension` | **The main one** — edit + submit the App Store listing, manage versions | Partner Dashboard → Settings → Members → invite; ensure role covers the app's Distribution/listing |
| 2 | **Vercel team `keyuchen1735-boops-projects`** | Member access to projects `shopify-app` (+ `calderyn-waitlist`) | Env vars, domains, redeploys, logs | Vercel → Team Settings → Members → invite |
| 3 | **GitHub `keyuchen1735-boop/calderyn-shopify-app`** | Write (collaborator) | Commit the toml, revert `.env.example`, future fixes | GitHub repo → Settings → Collaborators |
| 4 | **Squarespace** (registrar for `calderyncompany.com`) | DNS edit access (or be on-call to add records) | If review needs more subdomains / verification TXT | Squarespace domain → Permissions, or share via owner |
| 5 | **Supabase org / project `Calderyn-SHOPIFY`** (`ajgrmnvzxfxxlwrxcgnu`) | Member access | If touching data, migrations, or prod env parity | Supabase → Org → Members |
| 6 | **Anthropic console** | Access to rotate keys (or owner rotates) | Rotate the leaked key | Anthropic console |

---

## Reference — key facts & IDs

- **Production app URL:** `https://app.calderyncompany.com`
- **Shopify app:** `CalderynExtension` · App ID `372352679937` · client_id `508429753f1e60bd48c4dadbe063a27e`
- **Partner dashboard:** `https://dev.shopify.com/dashboard/214825973/apps/372352679937`
- **Released version:** `calderynextension-8`
- **Dev/test store:** `calderyn-shop-tester.myshopify.com`
- **Vercel:** team `keyuchen1735-boops-projects`, project `shopify-app`, prod alias was `shopify-app-rho-ruby.vercel.app`
- **Domain/DNS:** registrar **Squarespace**; `app`/`@`/`www` all `A → 76.76.21.21`; Workspace email lives on this zone (MX/DKIM — don't disturb)
- **Config file:** `shopify.app.calderynextension.toml` (single named config `calderynextension`)

## Cleanup note (this machine)
During the DNS investigation, three Google accounts were authed in `gcloud` (`keyuchen@calderyncompany.com`, `keyuchen1735@gmail.com`, `mezohyt@gmail.com`). Run `gcloud auth revoke <email>` for any you don't want lingering. The Cloud DNS API was enabled then reverted on project `seraphic-rune-498701-r8` — no net change.
